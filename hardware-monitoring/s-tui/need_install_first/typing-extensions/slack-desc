# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.  Line
# up the first '|' above the ':' following the base package name, and the '|' on
# the right side marks the last column you can put a character in.  You must make
# exactly 11 lines for the formatting to be correct.  It's also customary to
# leave one space after the ':'.

                 |-----handy-ruler------------------------------------------------------|
typing-extensions: The typing_extensions module serves two related purposes:
typing-extensions:
typing-extensions: Enable use of new type system features on older Python versions.
typing-extensions: Enable experimentation with new type system PEPs before they are
typing-extensions: accepted and added to the typing module.
typing-extensions:
typing-extensions:
typing-extensions:
typing-extensions:
typing-extensions:
typing-extensions:
